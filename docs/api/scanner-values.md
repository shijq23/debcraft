# Scanner Values

Scanner values are immutable dataclasses representing artifact descriptors,
identified packages, enrichment metadata, and scan results. These carry no
behavior beyond field access and are produced by scanner implementations and
enrichment services.

::: debcraft.domain.scanner.values
    options:
      show_root_heading: true
      members_order: source
      docstring_style: google
