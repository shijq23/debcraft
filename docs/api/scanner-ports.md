# Scanner Ports

Scanner ports define the protocol interfaces that infrastructure adapters must
implement. These allow the domain layer to remain decoupled from concrete
implementations of artifact scanning, contents indexing, package lookup, and
disk image inspection.

::: debcraft.domain.scanner.ports
    options:
      show_root_heading: true
      members_order: source
      docstring_style: google
